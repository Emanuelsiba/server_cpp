# this is the build file for project 
# it is autogenerated by the xmake build system.
# do not edit by hand.

# project
cmake_minimum_required(VERSION 3.15.0)
cmake_policy(SET CMP0091 NEW)
project(server_cpp LANGUAGES CXX)
include_directories(include)

# target
add_executable(server_cpp ""
        include/server.hpp
        src/server.cpp
)
set_target_properties(server_cpp PROPERTIES OUTPUT_NAME "server_cpp")
set_target_properties(server_cpp PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/build/linux/x86_64/release")
target_include_directories(server_cpp SYSTEM PRIVATE
    /home/qwerty/.xmake/packages/c/cpp-httplib/v0.22.0/d83e67dbf219462a9246234a666dd1ef/include
)
target_compile_options(server_cpp PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-m64>
    $<$<COMPILE_LANGUAGE:CXX>:-m64>
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
)
if(MSVC)
    target_compile_options(server_cpp PRIVATE $<$<CONFIG:Release>:-Ox -fp:fast>)
else()
    target_compile_options(server_cpp PRIVATE -O3)
endif()
if(MSVC)
else()
    target_compile_options(server_cpp PRIVATE -fvisibility=hidden)
endif()
if(MSVC)
    set_property(TARGET server_cpp PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endif()
target_link_options(server_cpp PRIVATE
    -m64
)
target_sources(server_cpp PRIVATE
    src/main.cpp
)

